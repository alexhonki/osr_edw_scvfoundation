PROCEDURE	"osr.scv.foundation.db.Procedures::SP_TraverseSubgroupsGraph"  
			(
			--	OUT outTab table (
			--		"MATCH_ROW" INTEGER
			)
	
LANGUAGE GRAPH     
READS SQL DATA
AS
BEGIN

	Graph g = Graph("osr.scv.foundation.db.data::MatchTracing");
	
	Multiset<vertex> v = Vertices(:g);
	
	Multiset<Vertex> n = Neighbors(:g, :v, 0, 5);
	
--	outTab = SELECT :e."MATCH_ROW" FOREACH e IN :n;

END;
